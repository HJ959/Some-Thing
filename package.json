{
  "name": "Something",
  "version": "1.0.0",
  "description": "Digital storytelling piece",
  "private": "true",
  "scripts": {
    "clean": "rm -r dist/*",
    "env": "env",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib && webpack && rm -r lib"
  },
  "author": "Henry James",
  "license": "ISC",
  "dependencies": {
    "three": "^0.138.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.6",
    "@babel/core": "^7.17.8",
    "webpack": "^5.70.0",
    "webpack-cli": "^4.9.2"
  }
}
